import { defineConfig } from 'vite';

export default defineConfig(async () => {
  // Move all top-level async / await calls inside this function body
  
  return {
    // Paste your existing Vite config object here (plugins, resolve, server, etc.)
  };
});
